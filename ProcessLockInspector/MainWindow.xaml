<Window x:Class="ProcessLockInspector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProcessLockInspector"
        mc:Ignorable="d"
        Title="Process Lock Inspector" Height="680" Width="1100" MinHeight="520" MinWidth="840">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Find processes locking a file/folder, then kill selected processes or rename now/at reboot."
                   FontSize="14"
                   Margin="0,0,0,10" />

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Target:" />
            <TextBox Grid.Column="1" x:Name="TargetPathTextBox" Height="30" VerticalContentAlignment="Center" Margin="0,0,8,0" />
            <Button Grid.Column="2" x:Name="BrowseFileButton" Content="Browse File" Height="30" Padding="12,0" Margin="0,0,8,0" Click="BrowseFileButton_Click" />
            <Button Grid.Column="3" x:Name="BrowseFolderButton" Content="Browse Folder" Height="30" Padding="12,0" Margin="0,0,8,0" Click="BrowseFolderButton_Click" />
            <Button Grid.Column="4" x:Name="AnalyzeLocksButton" Content="Analyze Locks" Height="30" Padding="14,0" Click="AnalyzeLocksButton_Click" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="New Name:" />
            <TextBox Grid.Column="1" x:Name="NewNameTextBox" Height="30" VerticalContentAlignment="Center" Margin="0,0,8,0" />
            <Button Grid.Column="2" x:Name="RenameNowButton" Content="Rename Now" Height="30" Padding="12,0" Margin="0,0,8,0" Click="RenameNowButton_Click" />
            <Button Grid.Column="3" x:Name="ForceRenameButton" Content="Force Rename on Reboot" Height="30" Padding="12,0" Click="ForceRenameButton_Click" />
        </Grid>

        <DataGrid Grid.Row="3"
                  x:Name="ProcessGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  Margin="0,0,0,8"
                  SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Kill?" Binding="{Binding MarkForKill, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70" />
                <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Name" Binding="{Binding ProcessName}" Width="200" IsReadOnly="True" />
                <DataGridTextColumn Header="Type" Binding="{Binding AppType}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="Main Window" Binding="{Binding MainWindowTitle}" Width="220" IsReadOnly="True" />
                <DataGridTextColumn Header="Path" Binding="{Binding ExecutablePath}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="SelectAllButton" Content="Select All" Height="30" Padding="12,0" Margin="0,0,8,0" Click="SelectAllButton_Click" />
            <Button x:Name="SelectNoneButton" Content="Select None" Height="30" Padding="12,0" Margin="0,0,8,0" Click="SelectNoneButton_Click" />
            <Button x:Name="KillSelectedButton" Content="Kill Selected" Height="30" Padding="12,0" Margin="0,0,8,0" Click="KillSelectedButton_Click" />
            <TextBlock x:Name="SummaryTextBlock" VerticalAlignment="Center" />
        </StackPanel>

        <TextBox Grid.Row="5"
                 x:Name="StatusTextBox"
                 Height="120"
                 FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True" />
    </Grid>
</Window>
